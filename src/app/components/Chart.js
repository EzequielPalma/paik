const Chart = ({ position, material }) => (
    <a-box 
     position={position}
     rotation="0 45 0"
     scale="0.5 4 5.5"
     material={material}
     grow
     
     > 
    </a-box>
)

 export default Chart